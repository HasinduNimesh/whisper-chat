{
  "name": "@private-chat/widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Embeddable chat loader: one <script> tag adds a floating chat bubble backed by an isolated iframe.",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.json",
    "dev": "vite build --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^8.1.1",
    "vitest": "^4.1.9"
  }
}
