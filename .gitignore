node_modules/
dist/
build/

# Machine-local Claude Code settings (shared agents/ + settings.json are kept)
.claude/settings.local.json
*.log
.DS_Store
.env
.env.local
coverage/
playwright-report/
test-results/
*.tsbuildinfo

# Vite bundles the client; tsc is type-check only there. Guard against stray
# compiled output ever shadowing .ts/.tsx sources (Vite resolves .js first).
client/src/**/*.js
